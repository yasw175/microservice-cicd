# CI/CD Pipeline for Microservices (Docker, GitHub Actions, REST APIs)

This project demonstrates a complete, automated CI/CD pipeline for a microservice architecture.

It uses two simple Python Flask services, containerizes them with Docker, and uses GitHub Actions to automate testing, building, and "deploying" them.

## Project Structure